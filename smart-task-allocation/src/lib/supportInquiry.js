// Shared between the "Contact Support" submission form and the Platform
// Admin Inquiries list, so the category a user picks always matches one of
// the filter options an admin can select — keep both in sync from here.
export const SUPPORT_INQUIRY_SUBJECTS = ["Billing", "Bug Report", "Account", "Feature Request", "Feedback", "Other"];
